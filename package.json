{
  "name": "prospection-app",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n BACKEND,FRONTEND -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && node src/server.js",
    "dev:frontend": "cd frontend && npx vite"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
